<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>AthletixOS</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
	</array>
	<!-- Photo uploads (profile photos, documents) use the web file picker,
	     which offers Camera and Photo Library. iOS kills the app without these. -->
	<key>NSCameraUsageDescription</key>
	<string>AthletixOS uses the camera so you can take a profile photo or photograph a document to upload.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>AthletixOS lets you choose a photo to upload as a profile picture or document.</string>
	<!-- Only standard HTTPS — answers App Store Connect's export-compliance question. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!-- App Transport Security exceptions.
	     - NSAllowsLocalNetworking lets the WebView reach `localhost`,
	       `127.0.0.1`, and any RFC1918 / link-local LAN address. Covers
	       the simulator (127.0.0.1:3000) AND a real iPhone hitting the
	       Mac's LAN IP (e.g. 10.0.0.x:3000). App Store safe; iOS 10+.
	     - NSAllowsArbitraryLoadsInWebContent keeps the rest of the app's
	       HTTPS-only behavior intact but lets WKWebView load mixed/HTTP
	       URLs you reach by following a link (useful for dev tunnels).
	     For production builds, server.url should already be HTTPS, so
	     these keys become no-ops — they're only relevant during local
	     development against `http://127.0.0.1:3000` or a LAN IP. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
		<key>NSAllowsArbitraryLoadsInWebContent</key>
		<true/>
	</dict>
</dict>
</plist>
